# Kunden-Analyse Dashboard - Frontend
# React/Vite Static Site

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run preview -- --host 0.0.0.0 --port $PORT"

[build.env]
NODE_ENV = "production"

